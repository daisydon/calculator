# Build Environment
build_environment: OS X 10.9.2

# language setting
language: node_js

# version numbers, testing against two versions of node
node_js:
  - 0.10.2

after_script:
- nodeunit ./TestCalculatorAdd.js

